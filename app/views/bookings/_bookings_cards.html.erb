<div class="container">
  <% @bookings.each do |booking| %>
    <% if booking.user == @user %>
      <% if Time.now > booking.end_date %>
          <div class='row border-bottom'>
            <div class=col-12>
              <div class="card-product" style='margin: 20px'>
                <%= cl_image_tag(booking.pet.photos.first.key) %>
                <div class="card-product-infos ml-4">
                  <h2><%= booking.pet.name %></h2>
                  <p><%= booking.start_date.strftime("%a, %d %B, %Y") %> - <%= booking.end_date.strftime("%a, %d %B, %Y") %> </p>
                    <br>
                    <%= link_to 'Add a review', new_booking_review_path(booking), class: 'btn btn-pets my-1' %>
                    <%= link_to 'Hide booking', booking_path(booking), method: :delete, data: { confirm: "Are you sure?" }, class: 'btn btn-ghost my-1' %>
                </div>
              </div>
            </div>
          </div>
      <% end %>
    <% end %>
  <% end %>
</div>
