<% @pets.each do |pet| %>
  <%= pet.name %>
  <% pet.photos.each do |photo| %>
  <%= cl_image_tag photo.key, height: 300, width: 400, crop: :fill %>
<% end %>
  <br>
<% end %>
<br>

<%= link_to "Rent my pet", new_pet_path, class: "btn btn-primary" %>
